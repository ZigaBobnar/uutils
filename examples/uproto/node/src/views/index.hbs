<div class="container">
  <h4>Connected devices:</h4>
    {{#unless devices.length}}
      No devices.
    {{/unless}}

    {{#if devices.length}}
      <ul>
        {{#each devices}}
          <li><a href="/devices/{{name}}">{{name}}</a></li>
        {{/each}}
      </ul>
    {{/if}}

</div>
